/**
 * Test for publicReadPolicy.
 * Runs with mocha.
 */
'use strict'

const publicReadPolicy = require('../lib/policies/publicReadPolicy')
const { ok, strictEqual: equal, deepStrictEqual: deepEqual } = require('assert')

describe('public-read-policy', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
