/**
 * Test for TheAzureStorage.
 * Runs with mocha.
 */
'use strict'

const TheAzureStorage = require('../lib/TheAzureStorage')
const { ok, equal } = require('assert')

describe('the-azure-storage', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    ok(TheAzureStorage)
  })
})

/* global describe, before, after, it */
